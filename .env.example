# ClassVerse Environment Variables

# Gemini API Key (Server-side)
GEMINI_API_KEY=

# Firebase Config (Shared/Public)
# These will be provided by the set_up_firebase tool and stored in firebase-applet-config.json
# You don't need to manually set them here.
